  hello hope help currently working  project trying communicate ibm  phoenix bio board intel cpu  uart chip board work fine transmit command  terminal emulation mode kermit wrong  initialization protocol try  access unit built sizable chunk  code perform calculation operation  difficult code assembly language   assembly code running code  trying show  blatantly incorrectly missing lack  knowledge serial communication please mail  wrote assembly language wait character received  check character received  led indicate program written  set com port send character  bioscom function bio software interrupt  int function triple checked baud rate  parity stop bit character length bit  interrupt call currently loss  wrong hardware guru comment    alot  jesus     asm   command equ command register  porta equ port  timerlo equ bit timer  timerhus equ low bit timer  uartda equ uart datum register  uartcmd equ uart command register   org sram location  mvus set low timer bit  sta timerlo  mvus set timer bit  sta timerhus  mvus start timer enable port  sta command   mvus display segment led  sta porta   mvus clear uart command  sta uartcmd  sta uartcmd  sta uartcmd  mvus internally reset uart  sta uartcmd  lda uartda remove extraneous datum  mvus init uart datum bit  sta uartcmd parity stop bit async  mvus enable transmit receive  sta uartcmd   anus rxrdy  init loop   lda uartda read datum character  cpus character  jnz init loop   mvus character received  sta porta display segment led  hlt  end   bioscom   #include stdio  #include stdlib  #include bio   #define init  #define send  #define receive  #define status   #define com   void  main   char abyte   abyte  bioscom init abyte com  printf initialized comm port   bioscom status com   abyte  bioscom send abyte com  printf start character\n     int   #include stdio  #include stdlib  #include  #include bio   main   union reg register   register  register  register  int &registers &registers  printf com initialized\n   register  register  register  int &registers &registers  printf start character\n    