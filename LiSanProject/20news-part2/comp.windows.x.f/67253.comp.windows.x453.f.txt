   article mark lanzo write   bin echo csh built echo  embed literal esc bel character string  convenient octal sequence   bad idea usr bin echo slower built echo  doe execute hostname shell window  doe read extra file manipluating title doe  require executing extra program   yes doe execute program system  store file time    hostname  hostname hostname  endif   figure generate escape bell  echo command line terminator  done variable set   executed script system   set echo variable dev tty  echo ${hostname}  echo ${hostname}  echo |wc  built echo bsd dev tty  berkeley style echo  set ech echo echo ${hostname}  set echo echo ${hostname}  set echo echo ${hostname}  set echo ${hostname}  echo ${hostname}   built echo sysv dev tty  set ech echo echo ${hostname}  set echo ${hostname}  set echo ${hostname}  set echo ${hostname}  echo ${hostname}     shelltool cmdtool xterm  duplicate alias avoid problem  $term sun  sun alias  header ${ech} ${e} ${e}\${n}  iheader ${ech} ${e} ${e}\${n}  $term xterm  header ${ech} ${e} ${b}${n}  iheader ${ech} ${e} ${b}${n}  endif      bruce barnett uunet crdgw barnett   